

<!-- header area start -->
<div class="header-area-five header-bg-five">
    <div class="container">
        <div class="row">
            <div class="col-lg-7">
                <div class="header-inner "><!-- header inner -->
                    <span class="subtitle ">{{$slider->subtitle ?? ''}}</span>
                    <h1 class="title ">{{$slider->title ?? ''}}</h1>
                    <p class="wow fadeInDown">{{$slider->second_subtitle ?? ''}}</p>
                    <div class="btn-wrapper wow fadeInDown">
                        <a href="{{$slider->button_link ?? ''}}" class="boxed-btn">{{$slider->button_text ?? ''}}</a>
                    </div>
                </div><!-- //. header inner -->
            </div>
        </div>
    </div>
</div>
<!-- header area end -->
